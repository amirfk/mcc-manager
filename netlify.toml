[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Daily automatic metrics snapshot into Supabase (06:00 UTC). No client needed.
[functions."scheduled-snapshot"]
  schedule = "0 6 * * *"
